# Node groups and bootstrap contract theory understood

User completed Lesson 3 covering managed vs self-managed node groups in detail, the complete bootstrap contract (baked-in binaries, cluster parameters via user data, infrastructure requirements), nodeadm and NodeConfig YAML, user data formats (plain vs MIME multi-part), launch template structure, and eksctl config for self-managed node groups with custom AMIs. User understands that self-managed node groups are the right choice for bootc AMIs due to no AMI family restriction. Ready for hands-on deployment.
